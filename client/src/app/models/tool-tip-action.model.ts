// Cuurently adding icon name for material icons, if needed class should also be included here
// which will allow custom images, other that material fonts one
export interface ToolTipAction {
    iconName: string, // Material icons name
    name: string
}